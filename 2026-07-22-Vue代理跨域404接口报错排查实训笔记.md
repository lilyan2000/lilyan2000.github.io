# Vue代理404接口报错实训笔记
## 一、故障现象
网页后台所有按钮点击无响应，浏览器控制台接口持续报404，接口请求全部访问前端8081端口，无法正常连通后端服务。

## 二、问题根源
1. 全局请求封装文件`http.js`中，axios接口请求前缀硬编码为`/zhinengxiaochengxsc`；
2. Vue项目代理规则仅拦截`/api`开头的请求，旧路径无法匹配代理，不会转发至后端8080端口，前端本身不提供业务接口，因此全部请求404。

## 三、解决方案
1. 修改`http.js`，将axios全局统一请求基地址改为`/api`，使所有业务接口匹配代理拦截规则；
2. 在`vue.config.js`中新增`/zhinengxiaochengxsc`代理配置，兼容页面内写死路径的图片、静态页面，防止页面样式、资源加载空白；
3. 修改`vue.config.js`代理配置后，必须彻底重启前端项目，浏览器清空缓存或使用无痕窗口访问页面。

## 四、代理核心原理
前端发起`/api`开头接口请求
→ Vue开发服务器拦截匹配代理规则
→ 自动将路径前缀`/api`替换为后端项目上下文根路径
→ 转发请求至后端8080端口，完成前后端接口调用

## 五、实操注意事项
1. 登录页面必须选中对应角色，账号密码与所选角色匹配才能登录成功；
2. 后端SpringBoot服务需全程保持运行状态，关闭后端会再次出现接口报错。
